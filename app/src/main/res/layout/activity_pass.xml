<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_pass"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:mContext="com.doandstevensen.lifecollage.ui.pass.PassActivity">

    <Button
        android:id="@+id/toggleBluetoothButton"
        android:text="Toggle Bluetooth"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/toggleBluetoothButton"
        android:id="@+id/toggleDiscoverable"
        android:text="Discoverable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:layout_below="@+id/toggleBluetoothButton"
        android:id="@+id/discoverDevicesButton"
        android:text="Discover Devices"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <Button
        android:layout_below="@+id/discoverDevicesButton"
        android:id="@+id/connectionButton"
        android:text="START CONNECTION"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:layout_below="@+id/connectionButton"
        android:id="@+id/collageSpinnerLabel"
        android:text="Collage To Pass:"
        android:textSize="15dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Spinner
        android:layout_below="@id/collageSpinnerLabel"
        android:id="@+id/collageSpinner"
        android:layout_width="match_parent"
        android:layout_height="50dp"/>

    <ListView
        android:layout_marginTop="15dp"
        android:layout_below="@+id/collageSpinner"
        android:layout_above="@+id/sendButton"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/lvNewDevices"/>

    <Button
        android:layout_alignParentBottom="true"
        android:id="@+id/sendButton"
        android:text="Send"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>
